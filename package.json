{
  "name": "nx-dsl",
  "displayName": "Nexus DSL (.nx) Language Support",
  "description": "Syntax highlighting, formatting and linting for the .nx DSL.",
  "version": "0.0.1",
  "publisher": "REPLACE_ME_PUBLISHER",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "keywords": [
    "nx",
    "dsl",
    "nexus"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "nx",
        "aliases": [
          "Nexus DSL",
          "nx"
        ],
        "extensions": [
          ".nx"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "nx",
        "scopeName": "source.nx",
        "path": "./syntaxes/nx.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "nx",
        "path": "./snippets/nx.code-snippets"
      }
    ],
    "configuration": {
      "title": "Nexus DSL",
      "properties": {
        "nxDsl.executablePath": {
          "type": "string",
          "default": "nx-dsl",
          "description": "Path to the nx-dsl CLI binary (build it from open-nexus-OS: cargo build -p nx-dsl). Used for formatting and diagnostics."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.24.0",
    "typescript": "^5.4.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/open-nexus-OS/nx-dsl-vscode.git"
  }
}
