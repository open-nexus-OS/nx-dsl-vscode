# Changelog

## Unreleased

- Full TextMate grammar for the `.nx` v1 surface.
- Formatting via `nx-dsl fmt` and diagnostics via `nx-dsl lint`, both shelling out to a configurable `nxDsl.executablePath`.

## 0.0.1

- Initial scaffold: language registration for `.nx`, placeholder syntax highlighting (comments + strings), build/publish CI.
